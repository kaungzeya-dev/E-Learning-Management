# Set Railway service "Root Directory" to: frontend
# Set variable VITE_API_BASE_URL before deploy (see Railway guide).
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
